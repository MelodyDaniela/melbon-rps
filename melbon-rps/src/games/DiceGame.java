package games;

import java.util.Scanner;

public class DiceGame {

/*
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        Dice dice = new Dice(6);
        Player nadja = new Player("Nadja", 0);
        int result = dice.roll();


        for (int i = 0; i < 3; i++) {
            int guess = 0;
            int wrongGuess = 0;
            boolean guessIsOutOfBounds = true;


            while (guessIsOutOfBounds) {
                System.out.println("Guess what the dice rolled");
                guess = scanner.nextInt();

                if (guess >= 1 && guess <= 6) {
                    guessIsOutOfBounds = false;
                }
                if (wrongGuess <1 && wrongGuess >= 7){
                    System.out.println("You need to enter a digit between 1 -6, Try again ");
                }
            }
            System.out.println("You guessed: " + guess);

            boolean sant = result == guess;
            // boolean falskt = result !=guess;

            if (sant) {
                System.out.println("Dice rolled: " + result + "\n" + "Congratulations, you guessed right!");
                break;
            } else {
                System.out.println("Dice rolled: " + result + "\n" + "Wrong guess, You have " + (2 - i) + " guesses left!");

            }
        }
    }
*/

}

